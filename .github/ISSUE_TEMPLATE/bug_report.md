---
name: Bug report
about: The harness did something other than what the specification says
title: ""
labels: bug
---

**What happened, and what the specification says should happen** (cite the chapter of
`oam_targetpicture.md` if you can).

**Entry point and arguments** — e.g. `run.bat p03_python_large m01_process`.

**Constants** — model, effort, repeats, campaign label, and whether a non-default `--config` was
used.

**Exit code.**

**Relevant log lines** from the run directory (`run.log`) or `local/runs/_master.log`. Please do not
paste whole run directories.

**Environment** — Windows version, `py -0` output, `claude --version`.
