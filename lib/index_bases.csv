# Frozen bases for the idx_* columns (oam_targetpicture.md chapter 13.1b).
#
# One base per column, global -- never per project and never per engine, so two idx_ cells always
# mean the same thing. Anchored on p04_python_xlarge under e03_claude_opus_5, the densest cell in
# the table (n=48), then rounded hard: a base is a yardstick a reader can quote, not a measurement.
# 1.0 therefore reads as "the typical Claude run of p04", and a smaller task lands above 1.0
# because it IS smaller. Ordering inside a project is untouched by the choice of base -- a constant
# divisor cannot reorder anything -- which is why five decimals are written.
#
# direction: down = smaller is better (index = base / value), up = larger is better (value / base).
# group:     effort  -> factor of idx_effort, engine-neutral columns only (no money)
#            quality -> factor of idx_quality, and only measures NOT already inside res_mi
#            solo    -> own index column, no composite
#
# Changing a base rewrites its whole column on the next consolidation. Do it deliberately and note
# the date here; do not tune one to make a result look better.
#
# 2026-09-14: first freeze, from 128 passing rows.
column,base,direction,group
prf_duration_s,200,down,effort
prf_turns,40,down,effort
tk_output,15000,down,effort
tk_cost_usd,1.0,down,solo
res_mi,15,up,quality
res_max_nesting,4,down,quality
res_max_func_sloc,20,down,quality
res_complexity,70,down,solo
res_sloc,200,down,solo
