Anchor. Pairs of directly contradictory instructions plus a done-criterion that cannot be checked.
If this does not score worse than `08_process_doctypes_roles_guardrails`, the apparatus is broken.
Carries no parameters and is never tuned.
Both halves of every pair must be harmful. A pair whose one half is a working guardrail hands the
agent that guardrail for free, which is how the anchor came to beat `00_empty` on parsimony.
