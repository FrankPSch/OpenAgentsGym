Tf. Test first: the test for the change written before the change; the agent's own tests are allowed and not scored.
Edit order is invisible in print mode, so adherence shows only in the outcome — res_score and res_score_holdout.
