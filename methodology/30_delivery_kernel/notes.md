DK. Delivery kernel: a real-world methodology transcribed into testbed features — the staged cycle
with a condition per stage, producer-never-certifies, the finding schema and its severity ladder,
restraint against over-delivery, reading discipline, escalation instead of guessing, and a fixed
return shape. Source: the EL Pipeline Signal-to-Model methodology (CLAUDE.md, kernel/operating
manual, planning, _creating_a_role, verifier, team_roles, document_types), 2026-09.

**Composite by intent, and the only one outside the ladder.** It cannot attribute an effect to a
single element; it answers a different question — does a methodology grown against real failures beat
`08_process_doctypes_roles_guardrails`, which was designed here, and by how much per token. Read
against `00_empty` for the effect and against `08` for whether transcribed practice beats the
incumbent. Seven of its eight side files are the existing byte-identical feature files; only
`cycle.md` is new, because no existing arm carries a staged cycle with an exit condition per stage.

What the source has and one run cannot carry, left out rather than written as text nobody can obey:
backlog, priority and size classes; job weights and the plain-instance control; the eleven-seat
roster with per-seat model and effort; dated files, draining and handovers; the kernel/project split;
the skills register; the method-change ledger. All of these need more than one session, more than one
seat, or a person to decide. Adherence checks: `res_artifacts` for the three documents,
`res_subagents_spawned >= 1` for the reviewer.
