DK. A working methodology's kernel, transcribed — composite by intent, outside the ladder: the
staged cycle with a condition per stage, producer-never-certifies, the finding schema and its
severity ladder, the discipline against over-delivery, escalation instead of guessing, a halt rule.
Source, read 2026-09: the EL Pipeline Signal-to-Model project — `CLAUDE.md` §2–§3 and the files it
itself calls its kernel (`operating manual.md`, `planning.md`, `verifier.md`, `_creating_a_role.md`,
the collaboration practices). Map: the entry file is the reading list, `doc_types=none` (an instance
reports; it writes no files) and *nothing ships on one mind*; `kernel.md` §1–§5 = manual §1 · manual
§4 with `verifier.md` · manual §5 · `CLAUDE.md` §3 · manual §2 with `_creating_a_role.md` Limits.
Excluded, because one agent on one task in one run cannot obey them: backlog, priority, size, the
work-order archive; weights, the roster, per-seat model and effort, the plain-instance control;
dated files, draining, handovers, seat knowledge bases; escalation to a person; the method-change
ledger. Adherence: `res_subagents_spawned >= 1`; no `res_artifacts` — no documents are asked for.
