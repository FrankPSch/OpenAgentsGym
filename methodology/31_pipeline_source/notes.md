PS. The pipeline source, untranscribed: the source project's own entry file deployed as it stands and
the directory it calls its portable kernel copied verbatim (`kernel/*.md` and the one skill), read
2026-09 from the EL Pipeline Signal-to-Model project. Two edits only: the version comment, and the
`00_methodology/kernel/` paths rewritten to `../methodology/` so the reading list resolves from the
project_workspace. Everything else the entry file names — `project_layout.md`, `team_roles.md`, the
briefings, `world_model.md` — is project, not kernel, and is not shipped; the agent finds those
references dangling, as a stranger cloning the kernel would. Deviations, declared: the entry file is
4 KB against the ~1 KB convention; it names the vendor's products and a Product Manager, an Owner
and role instances that do not exist in a run (rule 19.5); no `{{ }}` parameters, so
`definition_of_done`, `constraint_order` and `doc_types` record blank. Read against `30_delivery_kernel`:
the same kernel transcribed into the testbed frame at half the size. The question is whether the
transcription lost or added anything that moves the score, turns or cost.
