GSD-L. 41_gsd_core_full cut to a single run: the same cycle and the same producer-never-
certifies rule, with fifteen roles collapsed to six and eight artefacts to three (`.work/SPEC.md`,
`PLAN.md`, `REPORT.md`). Composed 2026-09 from 41 plus the ladder's own rungs — the finding schema
and the four-things-block ceiling from 30_delivery_kernel, the never-violate list from
15_invariants, the stall rule from 27_escalation.

Read against 41, the question is what the milestone scaffolding costs and buys. 41 asks for a
`.planning/` tree whose milestone, roadmap, research, backlog and learnings files have no reader
inside one run; 42 keeps only artefacts the next stage consumes and states the drop explicitly in
§1, so a run that produces them anyway is over-delivery and visible as such. Three rules are
sharpened rather than inherited: `out_of_scope` must be non-empty before planning (41 leaves the
scope fence implicit in a roadmap that does not exist here), only four things may block (41 lets a
reviewer's severity float), and the plan check is nine questions rather than thirteen dimensions,
dropping the four that only bind across phases.

Deviation from 41: no STATE file, no milestone close, no ship stage, no peer-review lane, no fast
lanes. Everything in 41 §10 survives, compressed into §9 as I6 and I7.

Adherence: `res_artifacts` = exactly three files under `.work/`; a fourth is over-delivery.
`res_subagents_spawned >= 3` with producer and checker never the same instance;
`res_files_added_src` and `res_diff_lines` read against I4 and §8.1; `res_tests_tampered` is I1.
