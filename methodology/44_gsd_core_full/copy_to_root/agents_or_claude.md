<!-- mth_version: 44_gsd_core_full.v1 -->

# Constraints

Read this section before you read the task. Constraint order: {{constraint_order=first}} — the
constraints bind whatever the task turns out to be.

Definition of done: {{definition_of_done=strict}}. Strict means: stop when the stated task is done.
Do not add files, tests, documentation or refactorings that neither the task nor this methodology
asked for.

Document types: {{doc_types=planning_tree}}. Planning tree means: the artefacts named in
`GSD Core - Faithful Methodology.md` §4 are written under `.planning/`, and no others. Review rounds:
{{review_rounds=3}}, capped, with the stall rule in §3.

**Nothing ships on one mind:** the instance that produced material never certifies it. Planner,
plan checker, executor and verifier are separate instances with separate contexts.

# What you read

`../methodology/GSD Core - Faithful Methodology.md`, one directory up from the project_workspace, is the reading list.
Nothing else unasked. Read the section for the stage you are entering before you enter it; do not
work from a remembered summary of it.

# Task

The task follows on standard input. Work in the current directory. When the constraints and the task
disagree, the constraints win; say so in your answer rather than silently choosing one.
