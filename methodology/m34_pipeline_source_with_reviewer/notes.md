PS+R. m31_pipeline_source plus the one project file that carries the source's reviewer rule, team_roles.md, copied verbatim and made
reachable (its path in the entry file rewritten to ../methodology/, as the kernel paths already are). Nothing else changes.
Read against 31: 31 spawned no reviewer (res_subagents_spawned=0) because the rule row 'nothing ships on one mind' lives in
team_roles.md, which 31 does not ship. The question is what the reviewer seat costs and buys. Same declared deviations as 31.
