GSD-S/L-p. 40_gsd_surface_lean with the vendor vocabulary in its side file replaced by generic wording (hook-event names,
tool names, command flags -> 'before a tool call', 'editing', 'the gaps mode') and its entry file made consistent with §1 of
the side file (the artefact list is §1's, not a shorter one). Same method, portable text (rule 19.5). Created because 40 was
edited after its first run; the rows of the two texts are kept apart. Read against 40: whether wording that names no vendor
changes anything.
